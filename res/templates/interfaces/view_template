package $PACKAGE_NAME$.ui.mvp.view.implementations;

import $PACKAGE_NAME$.ui.activities.$SCREEN_NAME$Activity;
import $PACKAGE_NAME$.ui.mvp.presenter.interfaces.$SCREEN_NAME$Presenter;
import $PACKAGE_NAME$.ui.mvp.view.interfaces.$SCREEN_NAME$View;
$BASE_COMPONENT_FULL_NAME$

public class $SCREEN_NAME$ViewImpl$BASE_COMPONENT_NAME$ implements $SCREEN_NAME$View {
    private $SCREEN_NAME$Presenter presenter;

    public $SCREEN_NAME$ViewImpl($SCREEN_NAME$Activity activity) {
        super(activity);
    }

    @Override
    public void setPresenter($SCREEN_NAME$Presenter presenter) {
        this.presenter = presenter;
    }
}

